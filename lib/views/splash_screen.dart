import 'package:flutter/material.dart';
class SplashSrcreen extends StatelessWidget {
  const SplashSrcreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
